<div class="panel panel-primary">
  <div class="panel-heading">Cadastrar tabela de preço</div>
  <div class="panel-body">
    <%= simple_form_for @category_price do |f| %>
      <%= f.association :category, label: 'Categorias' %>
      <%= f.input :rental_period, collection: [1,3,5,7,15,20,30], label: 'Prazo' %>
      <%= f.input :price, label: 'Preço' %>
      <%= f.submit 'Cadastrar', class: 'btn btn-primary'  %>
      <button type="button" class="btn btn-default">
        <%= link_to 'Voltar', contracts_path%>
      </button>
    <% end %>
  </div>
</div>
